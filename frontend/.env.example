# Frontend build-time config.
#
# In development this is left UNSET so the Vite dev server proxies /api to the
# local backend (http://localhost:8000). In production (Vercel) set it to the
# backend origin so the app calls the Render service directly.
#
# VITE_API_URL=https://sueca-prospect-radar-api.onrender.com
