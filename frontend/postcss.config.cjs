// Tailwind v4 is handled by the @tailwindcss/vite plugin, so no PostCSS
// plugins are needed here. This local (empty) config also prevents PostCSS
// from walking up to an unrelated config outside the project.
module.exports = { plugins: {} };
